"use client";

/**
 * User sync is now handled server-side in the auth middleware.
 * This component is kept as a no-op for backward compatibility.
 */
export function UserSyncEffect() {
  return null;
}
